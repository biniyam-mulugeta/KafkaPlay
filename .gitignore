# --- Secrets & local config -------------------------------------------------
.env
.env.local
config/clusters.yaml

# The University of Debrecen crest is NOT redistributable until permission is
# confirmed. The `offsetscope` theme falls back to a text wordmark when absent.
themes/offsetscope/logo.svg
themes/unideb/logo.svg

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node -------------------------------------------------------------------
node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# --- Test artefacts ---------------------------------------------------------
test-results/
playwright-report/
blob-report/
.playwright/

# --- Runtime data -----------------------------------------------------------
*.db
*.db-wal
*.db-shm
data/

# --- Editors / OS -----------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp
